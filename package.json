{
  "name": "azure-chaos",
  "version": "1.0.0",
  "description": "chaos orchestrator for the azure platform",
  "main": "dist/index.js",
  "typings": "dist/index",
  "scripts": {
    "lint": "tslint -p .",
    "compile": "npm run lint && tsc",
    "c-watch": "tsc --watch",
    "test": "npm run compile && ava",
    "t-watch": "ava --watch",
    "prepublish": "npm run test"
  },
  "ava": {
    "files": [
      "dist/test/**/*.js"
    ]
  },
  "author": "Ben Greenier",
  "license": "MIT",
  "devDependencies": {
    "@types/fs-extra-promise": "^1.0.4",
    "@types/temp": "^0.8.31",
    "ava": "^0.25.0",
    "fs-extra-promise": "^1.0.1",
    "temp": "^0.8.3",
    "tslint": "^5.9.1",
    "typescript": "^2.7.1"
  },
  "private": true
}
